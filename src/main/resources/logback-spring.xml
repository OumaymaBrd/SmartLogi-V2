<configuration scan="true">
    <property name="LOG_PATH" value="logs" />
    <property name="APP_LOG_FILE" value="${LOG_PATH}/smartspring.log" />
    <property name="EMAIL_LOG_FILE" value="${LOG_PATH}/debug-email.log" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender pour écrire tous les logs dans smartspring.log -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${APP_LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/smartspring-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender pour les logs email dans debug-email.log -->
    <appender name="EMAIL_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${EMAIL_LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/debug-email-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger spécifique pour EmailService -->
    <logger name="org.example.smartspring.service.EmailService" level="DEBUG" additivity="false">
        <appender-ref ref="EMAIL_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Logger pour javax.mail -->
    <logger name="javax.mail" level="DEBUG" additivity="false">
        <appender-ref ref="EMAIL_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Root logger écrit dans la console et le fichier -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
